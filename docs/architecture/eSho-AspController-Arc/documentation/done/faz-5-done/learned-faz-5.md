# Basket Service - İşlem Akış Diyagramları

Bu dokümanda Basket Service'te gerçekleştirilen iki temel işlemin detaylı akış diyagramları bulunmaktadır.

---

## 1. SEPETİ AÇMA (GetBasket) AKIŞI

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         GET /api/baskets/{userName}                          │
│                         Örnek: GET /api/baskets/alice                        │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketsController.cs (Satır 22-28)                                      │
│  ────────────────────────────────────────────────────────────────────────   │
│  • HTTP GET request'i alır                                                  │
│  • Route'dan userName parametresini okur                                   │
│  • MediatR'ye GetBasketQuery gönderir                                       │
│                                                                              │
│  Kod:                                                                        │
│  var basket = await _mediator.Send(new GetBasketQuery(userName));          │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  🔄 MediatR Pipeline Başlar                                                 │
│  ────────────────────────────────────────────────────────────────────────   │
│  MediatR, GetBasketQuery için handler bulur ve pipeline'ı başlatır         │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 LoggingBehavior.cs (Satır 16-29)                                        │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Request'i loglar: "Handling GetBasketQuery: {userName}"                  │
│  • Pipeline'ın ilk behavior'ı (en dış katman)                               │
│  • Sonraki behavior'a geçer                                                 │
│                                                                              │
│  Kod:                                                                        │
│  _logger.LogInformation("Handling {RequestName}: {@Request}", ...);        │
│  var response = await next();  // ← Sonraki behavior'a geçer                │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 ValidationBehavior.cs (Satır 16-40)                                     │
│  ────────────────────────────────────────────────────────────────────────   │
│  • GetBasketQuery için validator arar                                        │
│  • GetBasketQuery için validator YOK (sadece string parametresi)           │
│  • Validation atlanır, direkt handler'a geçer                               │
│                                                                              │
│  Kod:                                                                        │
│  if (_validators.Any()) { ... }  // ← false, validator yok                 │
│  return await next();  // ← Handler'a geçer                                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 GetBasketHandler.cs (Satır 28-63)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  Handler başlar:                                                             │
│                                                                              │
│  Adım 1: Redis'ten Sepeti Al                                               │
│  ────────────────────────────────────────────────────────────────────────   │
│  • IBasketRepository.GetBasket(userName) çağrılır                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketRepository.cs (Satır 18-26)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Redis'e bağlanır (IConnectionMultiplexer üzerinden)                     │
│  • Key: "basket:{userName}" (örn: "basket:alice")                           │
│  • IDatabase.StringGetAsync() ile Redis'ten veri çeker                      │
│  • JSON string'i ShoppingCart entity'sine deserialize eder                 │
│                                                                              │
│  Kod:                                                                        │
│  var basket = await _database.StringGetAsync($"basket:{userName}");         │
│  return JsonSerializer.Deserialize<ShoppingCart>(basket!);                  │
│                                                                              │
│  ⚠️ Eğer sepet yoksa → null döner                                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌─────────────────┴─────────────────┐
                    │                                   │
            Sepet VAR                          Sepet YOK
                    │                                   │
                    ▼                                   ▼
┌──────────────────────────────────┐  ┌──────────────────────────────────┐
│  GetBasketHandler.cs (Satır 33)  │  │  GetBasketHandler.cs (Satır 36)  │
│  ────────────────────────────────│  │  ────────────────────────────────│
│  if (basket == null) → FALSE     │  │  if (basket == null) → TRUE      │
│  Devam eder...                   │  │  Boş sepet DTO döner:            │
│                                  │  │  new ShoppingCartDto {           │
│                                  │  │    UserName = userName,           │
│                                  │  │    Items = []                     │
│                                  │  │  }                                │
└──────────────────────────────────┘  └──────────────────────────────────┘
                    │                                   │
                    │                                   │
                    └───────────┬───────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 GetBasketHandler.cs (Satır 43-52)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  Adım 2: Her Ürün İçin İndirim Sorgula (gRPC)                               │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Sepetteki her item için döngü başlar                                     │
│  • DiscountGrpcService.GetDiscount(productName) çağrılır                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 DiscountGrpcService.cs (Satır 20-44)                                     │
│  ────────────────────────────────────────────────────────────────────────   │
│  • DiscountProtoService.DiscountProtoServiceClient kullanır                 │
│  • GetDiscountRequest oluşturur: { ProductName = "iPhone 15" }             │
│  • gRPC çağrısı yapar: _client.GetDiscountAsync(request)                   │
│  • Discount.Grpc servisine HTTP/2 üzerinden gRPC request gönderir          │
│                                                                              │
│  ⚠️ Hata durumları:                                                          │
│  • RpcException (NotFound) → Amount = 0 döner (hata fırlatmaz)             │
│  • Diğer hatalar → Exception fırlatır                                       │
│                                                                              │
│  Kod:                                                                        │
│  var request = new GetDiscountRequest { ProductName = productName };        │
│  var response = await _client.GetDiscountAsync(request);                    │
│  return response;  // ← CouponModel { Amount = 10, ... }                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌─────────────────────────────────┐
                    │                                 │
            ┌───────┴───────┐              ┌──────────┴──────────┐
            │  Discount.Grpc │              │  Network (HTTP/2)      │
            │  Service       │◄─────────────│  gRPC Request          │
            │                │              │                        │
            │  GetDiscount() │              │  Response:             │
            │  metodu        │──────────────►│  CouponModel {         │
            │                │              │    Amount: 10,         │
            │                │              │    ProductName: ...    │
            │                │              │  }                      │
            └───────────────┘              └────────────────────────┘
                    │
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 GetBasketHandler.cs (Satır 47-51)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Her item için indirim miktarını toplar                                   │
│  • totalDiscount = coupon.Amount * item.Quantity                            │
│  • Örnek: Item1 (10₺ indirim × 2 adet) + Item2 (5₺ indirim × 1 adet)      │
│           = 20₺ + 5₺ = 25₺ toplam indirim                                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 GetBasketHandler.cs (Satır 54-57)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  Adım 3: Entity'den DTO'ya Map Et                                          │
│  ────────────────────────────────────────────────────────────────────────   │
│  • AutoMapper kullanılır                                                    │
│  • ShoppingCart → ShoppingCartDto                                           │
│  • ShoppingCartItem → ShoppingCartItemDto                                  │
│  • totalDiscount DTO'ya eklenir                                            │
│  • TotalPrice = basket.TotalPrice - totalDiscount (indirimli fiyat)        │
│                                                                              │
│  Kod:                                                                        │
│  var basketDto = _mapper.Map<ShoppingCartDto>(basket);                      │
│  basketDto.Discount = totalDiscount;                                         │
│  basketDto.TotalPrice = basket.TotalPrice - totalDiscount;                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 MappingProfile.cs (Satır 10-15)                                          │
│  ────────────────────────────────────────────────────────────────────────   │
│  • AutoMapper profile tanımları                                             │
│  • CreateMap<ShoppingCart, ShoppingCartDto>()                               │
│  • CreateMap<ShoppingCartItem, ShoppingCartItemDto>()                       │
│  • Property mapping'ler otomatik yapılır (aynı isimli property'ler)         │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 GetBasketHandler.cs (Satır 59-60)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Log kaydı: "Basket retrieved for alice: TotalPrice=175, Discount=25"    │
│  • ShoppingCartDto döner                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 LoggingBehavior.cs (Satır 25-27)                                        │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Response'u loglar: "Handled GetBasketQuery: {basketDto}"                │
│  • Pipeline'dan çıkar                                                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketsController.cs (Satır 26-27)                                      │
│  ────────────────────────────────────────────────────────────────────────   │
│  • MediatR'den ShoppingCartDto alır                                         │
│  • HTTP 200 OK response döner                                              │
│  • Response body: JSON formatında ShoppingCartDto                           │
│                                                                              │
│  Kod:                                                                        │
│  return Ok(basket);  // ← HTTP 200 + JSON body                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ✅ HTTP 200 OK Response                              │
│                         {                                                   │
│                           "userName": "alice",                              │
│                           "items": [...],                                   │
│                           "totalPrice": 175.00,                             │
│                           "discount": 25.00                                 │
│                         }                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. SEPETE ÜRÜN EKLEME (StoreBasket) AKIŞI

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         POST /api/baskets                                    │
│                         Body: { "userName": "alice", "items": [...] }       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketsController.cs (Satır 30-36)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  • HTTP POST request'i alır                                                 │
│  • Request body'den ShoppingCartDto okur ([FromBody])                       │
│  • MediatR'ye StoreBasketCommand gönderir                                   │
│                                                                              │
│  Kod:                                                                        │
│  var result = await _mediator.Send(new StoreBasketCommand(basket));        │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  🔄 MediatR Pipeline Başlar                                                 │
│  ────────────────────────────────────────────────────────────────────────   │
│  MediatR, StoreBasketCommand için handler bulur ve pipeline'ı başlatır      │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 LoggingBehavior.cs (Satır 16-29)                                        │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Request'i loglar: "Handling StoreBasketCommand: {basket}"                │
│  • Pipeline'ın ilk behavior'ı (en dış katman)                               │
│  • Sonraki behavior'a geçer                                                │
│                                                                              │
│  Kod:                                                                        │
│  _logger.LogInformation("Handling {RequestName}: {@Request}", ...);       │
│  var response = await next();  // ← Sonraki behavior'a geçer               │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 ValidationBehavior.cs (Satır 16-40)                                     │
│  ────────────────────────────────────────────────────────────────────────   │
│  Adım 1: Validator Bul ve Çalıştır                                          │
│  ────────────────────────────────────────────────────────────────────────   │
│  • StoreBasketCommand için validator arar                                   │
│  • DI Container'dan IValidator<StoreBasketCommand> alır                     │
│  • StoreBasketValidator bulunur ✅                                          │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 StoreBasketValidator.cs (Satır 6-22)                                    │
│  ────────────────────────────────────────────────────────────────────────   │
│  • FluentValidation kuralları çalıştırılır                                  │
│                                                                              │
│  Validation Kuralları:                                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  1. RuleFor(x => x.Basket)                                                  │
│     .NotNull() → Basket null olamaz                                         │
│                                                                              │
│  2. RuleFor(x => x.Basket.UserName)                                         │
│     .NotEmpty() → UserName boş olamaz                                       │
│                                                                              │
│  3. RuleFor(x => x.Basket.Items)                                            │
│     .NotNull() → Items null olamaz                                          │
│                                                                              │
│  4. RuleForEach(x => x.Basket.Items)                                        │
│     .SetValidator(new ShoppingCartItemValidator())                          │
│     → Her item için ShoppingCartItemValidator çalıştırılır                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 ShoppingCartItemValidator.cs (Satır 24-40)                              │
│  ────────────────────────────────────────────────────────────────────────   │
│  Her item için validation:                                                   │
│                                                                              │
│  • RuleFor(x => x.ProductId).NotEmpty()                                     │
│  • RuleFor(x => x.ProductName).NotEmpty()                                   │
│  • RuleFor(x => x.Quantity).GreaterThan(0)                                  │
│  • RuleFor(x => x.Price).GreaterThan(0)                                     │
│                                                                              │
│  ⚠️ Eğer validation başarısız olursa:                                        │
│     → ValidationException fırlatılır                                       │
│     → HTTP 400 Bad Request döner                                            │
│     → Handler'a geçilmez                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │                                   │
            Validation ✅                      Validation ❌
                    │                                   │
                    ▼                                   ▼
┌──────────────────────────────────┐  ┌──────────────────────────────────┐
│  ValidationBehavior.cs (Satır 39)│  │  ValidationBehavior.cs (Satır 35)│
│  ────────────────────────────────│  │  ────────────────────────────────│
│  return await next();             │  │  throw new ValidationException(  │
│  → Handler'a geçer               │  │    failures);                    │
│                                  │  │  → HTTP 400 Bad Request           │
└──────────────────────────────────┘  └──────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 StoreBasketHandler.cs (Satır 25-39)                                     │
│  ────────────────────────────────────────────────────────────────────────   │
│  Handler başlar:                                                             │
│                                                                              │
│  Adım 1: DTO'dan Entity'ye Map Et                                            │
│  ────────────────────────────────────────────────────────────────────────   │
│  • AutoMapper kullanılır                                                     │
│  • ShoppingCartDto → ShoppingCart                                            │
│  • ShoppingCartItemDto → ShoppingCartItem                                  │
│                                                                              │
│  Kod:                                                                        │
│  var basket = _mapper.Map<ShoppingCart>(request.Basket);                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 MappingProfile.cs (Satır 10-15)                                          │
│  ────────────────────────────────────────────────────────────────────────   │
│  • AutoMapper profile tanımları                                             │
│  • CreateMap<ShoppingCartDto, ShoppingCart>()                               │
│  • CreateMap<ShoppingCartItemDto, ShoppingCartItem>()                       │
│  • Property mapping'ler otomatik yapılır                                    │
│  • TotalPrice → ShoppingCart entity'sinde calculated property (otomatik)   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 StoreBasketHandler.cs (Satır 30-31)                                     │
│  ────────────────────────────────────────────────────────────────────────   │
│  Adım 2: Redis'e Kaydet                                                      │
│  ────────────────────────────────────────────────────────────────────────   │
│  • IBasketRepository.SaveBasket(basket) çağrılır                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketRepository.cs (Satır 28-36)                                       │
│  ────────────────────────────────────────────────────────────────────────   │
│  • ShoppingCart entity'sini JSON'a serialize eder                           │
│  • Key: "basket:{userName}" (örn: "basket:alice")                           │
│  • IDatabase.StringSetAsync() ile Redis'e kaydeder                          │
│  • Redis'ten tekrar okuyup doğrular (GetBasket)                             │
│                                                                              │
│  Kod:                                                                        │
│  var serializedBasket = JsonSerializer.Serialize(basket);                   │
│  await _database.StringSetAsync($"basket:{basket.UserName}", ...);         │
│  return await GetBasket(basket.UserName) ?? basket;                        │
│                                                                              │
│  📝 Redis'te saklanan format:                                                │
│  Key: "basket:alice"                                                         │
│  Value: {                                                                    │
│    "userName": "alice",                                                      │
│    "items": [                                                                │
│      { "productId": "1", "productName": "iPhone 15", ... }                  │
│    ]                                                                         │
│  }                                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌─────────────────────────────────┐
                    │                                 │
            ┌───────┴───────┐              ┌──────────┴──────────┐
            │     Redis      │              │  IConnectionMultiplexer│
            │   Database     │◄─────────────│  (Singleton)        │
            │                │              │                      │
            │  Key:          │              │  • Bağlantı havuzu   │
            │  "basket:alice"│              │  • Thread-safe       │
            │                │              │  • Paylaşılan bağlantı│
            │  Value: JSON   │              │                      │
            │  { ... }       │              │                      │
            └───────────────┘              └──────────────────────┘
                    │
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketRepository.cs (Satır 33)                                          │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Log kaydı: "Basket saved for alice"                                      │
│  • Saved ShoppingCart entity döner                                          │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 StoreBasketHandler.cs (Satır 33-34)                                     │
│  ────────────────────────────────────────────────────────────────────────   │
│  Adım 3: Entity'den DTO'ya Map Et                                           │
│  ────────────────────────────────────────────────────────────────────────   │
│  • AutoMapper kullanılır                                                    │
│  • ShoppingCart → ShoppingCartDto                                             │
│  • ShoppingCartItem → ShoppingCartItemDto                                   │
│                                                                              │
│  Kod:                                                                        │
│  var basketDto = _mapper.Map<ShoppingCartDto>(savedBasket);                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 StoreBasketHandler.cs (Satır 36)                                        │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Log kaydı: "Basket stored for alice"                                     │
│  • ShoppingCartDto döner                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 LoggingBehavior.cs (Satır 25-27)                                        │
│  ────────────────────────────────────────────────────────────────────────   │
│  • Response'u loglar: "Handled StoreBasketCommand: {basketDto}"            │
│  • Pipeline'dan çıkar                                                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  📁 BasketsController.cs (Satır 34-35)                                      │
│  ────────────────────────────────────────────────────────────────────────   │
│  • MediatR'den ShoppingCartDto alır                                         │
│  • HTTP 200 OK response döner                                              │
│  • Response body: JSON formatında ShoppingCartDto                           │
│                                                                              │
│  Kod:                                                                        │
│  return Ok(result);  // ← HTTP 200 + JSON body                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ✅ HTTP 200 OK Response                              │
│                         {                                                   │
│                           "userName": "alice",                              │
│                           "items": [                                        │
│                             {                                               │
│                               "productId": "1",                             │
│                               "productName": "iPhone 15",                   │
│                               "quantity": 2,                                │
│                               "price": 100.00                                │
│                             }                                               │
│                           ],                                                │
│                           "totalPrice": 200.00                              │
│                         }                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## ÖNEMLİ NOTLAR

### Pipeline Sırası (Her İki İşlem İçin)

```
1. LoggingBehavior (En dış katman)
   ↓
2. ValidationBehavior (Validation kontrolü)
   ↓
3. Handler (İş mantığı)
   ↓
4. LoggingBehavior (Response loglama)
```

### Dosya Rolleri Özeti

| Dosya | Rol | Ne Yapar |
|-------|-----|----------|
| **BasketsController.cs** | HTTP Endpoint | REST API endpoint'lerini tanımlar, MediatR'ye request gönderir |
| **LoggingBehavior.cs** | Cross-cutting | Tüm request/response'ları loglar |
| **ValidationBehavior.cs** | Cross-cutting | FluentValidation validator'ları otomatik çalıştırır |
| **GetBasketHandler.cs** | İş Mantığı | Sepeti Redis'ten alır, gRPC ile indirim sorgular, DTO'ya map eder |
| **StoreBasketHandler.cs** | İş Mantığı | DTO'yu Entity'ye map eder, Redis'e kaydeder, DTO'ya map eder |
| **BasketRepository.cs** | Veri Erişim | Redis CRUD işlemlerini yapar (Get, Save, Delete) |
| **DiscountGrpcService.cs** | gRPC Client | Discount servisine gRPC çağrısı yapar |
| **StoreBasketValidator.cs** | Validation | StoreBasketCommand için validation kuralları tanımlar |
| **ShoppingCartItemValidator.cs** | Validation | ShoppingCartItemDto için validation kuralları tanımlar |
| **MappingProfile.cs** | Mapping | AutoMapper profile tanımları (Entity ↔ DTO) |

### Teknoloji Kullanımı

- **MediatR**: CQRS pattern, request/response pipeline
- **FluentValidation**: Request validation (otomatik ValidationBehavior tarafından)
- **AutoMapper**: Entity ↔ DTO mapping
- **StackExchange.Redis**: Redis veritabanı erişimi
- **gRPC**: Discount servisi ile senkron iletişim
- **System.Text.Json**: JSON serialization/deserialization

---

## AKIŞ KARŞILAŞTIRMASI

| Özellik | GetBasket | StoreBasket |
|---------|-----------|-------------|
| **HTTP Method** | GET | POST |
| **Validation** | Yok (sadece string parametre) | Var (StoreBasketValidator) |
| **Redis İşlemi** | GetBasket (okuma) | SaveBasket (yazma) |
| **gRPC Çağrısı** | Var (her item için indirim) | Yok |
| **Mapping** | Entity → DTO | DTO → Entity → DTO |
| **Pipeline Behaviors** | LoggingBehavior | LoggingBehavior + ValidationBehavior |

---

## ⚠️ ÖNEMLİ: REDIS TEK VERİ KAYNAĞI

### Soru: Redis'te veri yoksa veya kayıt yapılmazsa, veritabanına (DB) bakılıyor mu?

**Cevap: HAYIR! Basket Service'te PostgreSQL veritabanı YOK.**

### Mimari Karar

```
┌─────────────────────────────────────────────────────────────┐
│                    BASKET SERVICE                           │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  BasketRepository.cs                                 │  │
│  │  ──────────────────────────────────────────────────  │  │
│  │  • GetBasket() → Redis'ten okur                      │  │
│  │  • SaveBasket() → Redis'e yazar                       │  │
│  │  • DeleteBasket() → Redis'ten siler                  │  │
│  │                                                       │  │
│  │  ⚠️ PostgreSQL YOK!                                  │  │
│  │  ⚠️ DbContext YOK!                                    │  │
│  │  ⚠️ SQL sorgusu YOK!                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                        │                                    │
│                        ▼                                    │
│              ┌─────────────────────┐                        │
│              │      REDIS          │                        │
│              │   (Tek Veri Kaynağı)│                        │
│              │                     │                        │
│              │  Key: "basket:user1"│                        │
│              │  Value: JSON       │                        │
│              └─────────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### Neden PostgreSQL Yok?

| Özellik | Açıklama |
|---------|----------|
| **Geçici Veri** | Sepet geçici veridir, kullanıcı çıkış yapınca silinebilir |
| **Hız** | Redis çok hızlı (in-memory), SQL DB'den daha hızlı |
| **Basitlik** | Key-Value yapısı sepet için ideal (`basket:user1` → JSON) |
| **Checkout Sonrası** | Checkout yapılınca sepet silinir, Ordering Service'e event gönderilir |
| **Kalıcılık Gerekmez** | Sepet kalıcı değil, sipariş kalıcı (Ordering Service'te) |

### Akış Senaryoları

#### Senaryo 1: Redis'te Veri Yok
```
1. GetBasket("alice") çağrılır
   ↓
2. BasketRepository.GetBasket() → Redis'ten okur
   ↓
3. Redis'te "basket:alice" key'i YOK
   ↓
4. GetBasketHandler → null döner
   ↓
5. Handler → Boş sepet DTO döner (hata fırlatmaz)
   ↓
6. ⚠️ PostgreSQL'e BAKILMAZ (çünkü DB yok)
```

#### Senaryo 2: Redis Down Olursa
```
1. GetBasket("alice") çağrılır
   ↓
2. BasketRepository.GetBasket() → Redis'e bağlanmaya çalışır
   ↓
3. Redis bağlantı hatası (ConnectionException)
   ↓
4. Exception fırlatılır → GlobalExceptionHandler yakalar
   ↓
5. HTTP 500 Internal Server Error döner
   ↓
6. ⚠️ PostgreSQL'e BAKILMAZ (çünkü DB yok)
```

#### Senaryo 3: Veri Kaybolursa
```
1. Redis restart olur (veri kaybolur - persistence yoksa)
   ↓
2. GetBasket("alice") çağrılır
   ↓
3. Redis'te veri YOK → null döner
   ↓
4. Handler → Boş sepet döner
   ↓
5. ⚠️ PostgreSQL'e BAKILMAZ (çünkü DB yok)
   ↓
6. Kullanıcı sepeti tekrar oluşturur
```

### Diğer Servislerle Karşılaştırma

| Servis | Veritabanı | Neden? |
|--------|------------|--------|
| **Catalog.API** | PostgreSQL | Ürünler kalıcı veri, ilişkisel yapı gerekli |
| **Basket.API** | **Redis (Tek Kaynak)** | Sepet geçici veri, hız önemli |
| **Ordering.API** | PostgreSQL | Siparişler kalıcı veri, ilişkisel yapı gerekli |
| **Discount.Grpc** | PostgreSQL | Kuponlar kalıcı veri, ilişkisel yapı gerekli |

### Redis Persistence (Opsiyonel)

Redis'te veri kaybını önlemek için persistence kullanılabilir:

```yaml
# docker-compose.yml
basketdb:
  image: redis/redis-stack:latest
  command: redis-server --appendonly yes  # AOF persistence
  volumes:
    - basketdb_data:/data  # Veri kalıcı olur
```

**Ancak:**
- Sepet geçici veri olduğu için persistence genelde gerekmez
- Checkout sonrası sepet silinir zaten
- Kullanıcı sepeti tekrar oluşturabilir

### Özet

✅ **Redis = Tek Veri Kaynağı** (PostgreSQL yok)  
✅ **Redis'te veri yoksa → null döner, DB'ye bakılmaz**  
✅ **Redis down olursa → Exception fırlatılır, DB'ye bakılmaz**  
✅ **Bu bir mimari karar → Sepet geçici veri için ideal**

---

*Bu dokümantasyon, Basket Service'in iç işleyişini anlamak için detaylı akış diyagramları içermektedir.*

